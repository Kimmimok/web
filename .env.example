# Example environment variables for local development and for hosting (do NOT commit real secrets)
REACT_APP_SHEET_APPEND_URL=https://script.google.com/macros/s/XXX/exec
REACT_APP_SHEET_APPEND_TOKEN=your_token_here
REACT_APP_USE_PROXY=true

# Optional (only if you still allow direct Google Sheets API reads in production)
# REACT_APP_SHEET_ID=your_sheet_id
# REACT_APP_API_KEY=your_public_api_key

# Optional: set homepage if hosting under a subpath
# PUBLIC_URL=/

# Notes:
# - Copy this file to .env for local development and fill real values.
# - Do NOT commit .env. The repo already ignores .env.
